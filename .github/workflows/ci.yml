name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-test:
    # macos-15 ships Xcode 16, which bundles the Swift Testing module.
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Build
        run: swift build

      - name: Test
        run: swift test
